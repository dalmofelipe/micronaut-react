import { styled, Box } from "@mui/material";

export const LoadingContainer = styled(Box)({
  width: '100%',
  minHeight: '50vh',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  margin: '0 auto',
});